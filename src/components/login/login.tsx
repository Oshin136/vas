import LoginForm from "../../forms/LoginForm";
import UserNavbar from "../navbar/UserNavbar";

function Login() {
  return (
    <div>
      <UserNavbar />
      <LoginForm />;
    </div>
  );
}

export default Login;
